{
  "form": {
    "required_str": "{{field}} is required",
    "is_email": "{{field}} is format email",
    "required_number": "{{field}} is required",
    "required_obj": "{{field}} is required",
    "required_ar": "{{field}} is required",
    "maxLength": "{{field}} is maximum {{length}} characters",
    "minLength": "{{field}} is min {{length}} characters",
    "is_half_size": ""
  }
}
